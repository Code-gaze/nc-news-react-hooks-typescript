import * as React from 'react';
import { Button } from '@material-ui/core';

interface IVoteProps {
}

const Vote: React.FunctionComponent<IVoteProps> = (props) => {
    const [vote, setVote]= React.useState(0)
  return ;
};

export default Vote;
